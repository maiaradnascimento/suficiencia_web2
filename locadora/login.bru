meta {
  name: login
  type: http
  seq: 1
}

post {
  url: http://localhost:8081/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "admin@locadora.com",
    "senha": "Admin123"
  }
}

settings {
  encodeUrl: true
}
